info:
  name: creation
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/thought
  body:
    type: multipart-form
    data:
      - name: thoughtText
        type: text
        value: " Even when a balloon is half inflated, it is completely full"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
